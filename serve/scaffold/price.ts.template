import type { Pricer } from "acp-cli/serve/types";

/**
 * Pricer — OPTIONAL
 *
 * Returns dynamic pricing based on the client's requirements.
 * Only called for ACP native flow — x402/MPP use the offering's fixed price.
 *
 * Use this when pricing depends on the request:
 * - Complexity-based pricing
 * - Volume discounts
 * - Time-of-day pricing
 *
 * The returned amount is in USDC. It overrides the offering's priceValue
 * for this specific job.
 *
 * Delete this file if your offering has a fixed price.
 */
const price: Pricer = async (input) => {
  // Example: charge based on complexity
  // const words = input.requirements.text?.split(" ").length ?? 0;
  // return { amount: Math.max(0.01, words * 0.001) };

  return { amount: input.offering.priceValue };
};

export default price;
