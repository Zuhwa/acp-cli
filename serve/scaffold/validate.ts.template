import type { Validator } from "acp-cli/serve/types";

/**
 * Validator — OPTIONAL
 *
 * Decides whether to accept an incoming job before pricing.
 * Only called for ACP native flow — x402/MPP clients are accepted
 * automatically if their requirements pass schema validation.
 *
 * Use this to enforce business rules beyond what the JSON schema covers:
 * - Content policy (reject offensive requests)
 * - Capacity limits (reject if too busy)
 * - Allowlist/blocklist by client address
 *
 * Delete this file if you don't need custom validation.
 */
const validate: Validator = async (input) => {
  // Example: reject if requirements contain blocked content
  // if (input.requirements.prompt?.includes("harmful")) {
  //   return { accept: false, reason: "Content policy violation" };
  // }

  return { accept: true };
};

export default validate;
